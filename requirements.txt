# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
aiofiles==23.2.1

# ── Database (async SQLite — no external DB needed) ────────────────────────────
aiosqlite==0.20.0

# ── Config ─────────────────────────────────────────────────────────────────────
pydantic-settings==2.3.4
python-dotenv==1.0.1

# ── Zoom Downloader ────────────────────────────────────────────────────────────
yt-dlp>=2024.5.27

# ── Google AI (Gemini — for direct audio + text summarization) ─────────────────
# ≥1.20 required: client.aio native async + pydantic response_schema support
google-genai>=1.20.0

# ── Transcription (Faster-Whisper — used only in WHISPER_LOCAL mode) ───────────
# Runs on CTranslate2 + onnxruntime — torch is NOT a dependency and is not
# installed anywhere (it was ~1.7 GB of dead weight in the old image).
faster-whisper==1.0.3

# ── Utilities ──────────────────────────────────────────────────────────────────
# httpx ≥0.28.1 required by google-genai ≥1.20
httpx>=0.28.1,<1.0.0
psutil==6.0.0

# ── Flashcards → Anki export ──────────────────────────────────────────────────
# Builds .apkg files students can import into the Anki spaced-repetition app.
genanki==0.13.1

# ── Testing ─────────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.8
anyio>=4.4.0,<5
