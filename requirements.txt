# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
aiofiles==23.2.1

# ── Database (async SQLite — no external DB needed) ────────────────────────────
aiosqlite==0.20.0

# ── Config ─────────────────────────────────────────────────────────────────────
pydantic-settings==2.3.4
python-dotenv==1.0.1

# ── Zoom Downloader ────────────────────────────────────────────────────────────
yt-dlp>=2024.5.27

# ── Google AI (Gemini — for direct audio + text summarization) ─────────────────
google-genai>=1.0.0

# ── Transcription (Faster-Whisper — used only in WHISPER_LOCAL mode) ───────────
# torch CPU-only is installed separately in Dockerfile to save ~1 GB of disk
faster-whisper==1.0.3

# ── Utilities ──────────────────────────────────────────────────────────────────
httpx==0.27.0
psutil==6.0.0

# ── LTI 1.3 (institutional SSO via Canvas / Moodle) ────────────────────────────
# PyJWT[crypto] pulls in `cryptography`, which we use both for JWT RS256 verify
# and for generating + serving our RSA-2048 tool key. No pylti1.3 dep — its
# only added value is a Flask/Django request adapter that doesn't fit FastAPI.
PyJWT[crypto]==2.9.0

# ── Flashcards → Anki export ──────────────────────────────────────────────────
# Builds .apkg files students can import into the Anki spaced-repetition app.
genanki==0.13.1

# ── PDF export (Task 5) ───────────────────────────────────────────────────────
# WeasyPrint needs system libs (Pango, Cairo) — installed in Docker via Dockerfile.
# Markdown converts Obsidian MD → HTML before WeasyPrint renders to PDF.
weasyprint>=61.0
Markdown>=3.6

# ── Testing ─────────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.8
anyio==4.4.0
